<?xml version="1.0" encoding="utf-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:QrCode_Reader.Views"
    x:Class="QrCode_Reader.AppShell">

    <TabBar>

        <ShellContent
            Title="Scanner"
            Icon="scan.png"
            ContentTemplate="{DataTemplate views:ScannerPage}" />

        <ShellContent
            Title="Lista"
            Icon="list.png"
            ContentTemplate="{DataTemplate views:ListPage}" />

    </TabBar>

</Shell>
